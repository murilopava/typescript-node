type calendarDate = [day: number, month: number, year: number]

const date: calendarDate = [18, 12, 2025]

function createDate(date: calendarDate) {
    const [day, month, year] = date
}

createDate(date)
